{% extends "base.html.twig" %}
    {% block body %}

        {# On affiche les messages flash que définissent les contrôleurs du bundle #}


        <div class="loginBack" align="center"  >
            <div align="center" class="card col-lg-12" style="width: 50rem; height: 50rem; ">
                <img src="{{ asset('img/suiviWatch.jpg') }}" class="card-img-top"  style="height: 50%;">
                <div class="card-body">
                    <h3 class="card-title">Bienvenue sur le portail de suivi de tâches</h3>
                    <img class="homeLogin" src="{{ asset('img/logo.png') }}">
                    <p class="card-text"></p>
                    <div class="col-lg-12" style="width: 14rem;">
                        {# On définit ce block, dans lequel vont venir s'insérer les autres vues du bundle #}
                        {% block fos_user_content %}{% endblock%}

                    </div>
                </div>
            </div>
        </div>
    {% endblock %}
